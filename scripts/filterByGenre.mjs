// want to filter films by genre by selecting genre in header
//  display them on home page with different banner

// get buttons from header by ID ✅
// console log the button ✅
// add eventListener to button ✅

// get the filter criteria
// filter based on genre

// generate different header to corresponding genre




















// refactor code from chatGPT:
// const genreMap = {
//     "js-genre-action": "action",
//     "js-genre-comedy": "comedy",
//     "js-genre-drama": "drama",
//     "js-genre-horror": "horror",
//     "js-genre-kids": "kids"
// };

// navGenreButtons.addEventListener('click', (event) => {
//     const genre = genreMap[event.target.id];
//     if (genre) {
//         console.log(`${genre} genre selected`);
//     }
// });
