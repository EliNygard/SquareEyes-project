// want to filter films by genre by selecting genre in header
//  display them on home page with different banner

// get buttons from header by ID
// console log the button
// add eventListener to button

// get the filter criteria
// filter based on genre

const actionGenreButton = document.getElementById("js-genre-action")
console.log(actionGenreButton);